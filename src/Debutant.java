import java.util.ArrayList;
import java.util.Stack;

public class Debutant extends Jeu{
    
    public Debutant(ArrayList<Carte> listeCartes, Stack<Carte> cartesTable, ArrayList<Joueur> joueurs) {
		super(listeCartes, cartesTable, joueurs);
	}


    public void jouer(){
        /** 
        for(int i = 0; i < listeCartes.size();i++){
            if(listeCartes.get(i).getNom().equals("Orange") ||
                listeCartes.get(i).getNom().equals("Vert") ||
                listeCartes.get(i).getNom().equals("Gris") ||
                listeCartes.get(i).getNom().equals("Noir") ||){

                }
        }
        */
    }

}
